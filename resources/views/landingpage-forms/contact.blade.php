<!-- CONTACT TAB -->
<div id="contact" class="tab-content p-5 md:p-10 min-h-[calc(100vh-100px)] animate-fadeIn">
    <div class="max-w-6xl mx-auto bg-white p-6 md:p-10 rounded-xl shadow-lg border border-slate-200">
        <h1 class="text-2xl md:text-3xl lg:text-4xl font-bold text-center text-slate-900 mb-6 md:mb-8">{{ __('contact.page_title') }}</h1>
        <p class="text-slate-700 text-base md:text-lg leading-relaxed"><strong>{{ __('contact.municipality_name') }}</strong></p>
        <p class="text-slate-700 text-base md:text-lg leading-relaxed">{{ __('contact.address_label') }} {{ __('contact.address') }}</p>
        <p class="text-slate-700 text-base md:text-lg leading-relaxed">{{ __('contact.phone_label') }} {{ __('contact.phone') }}</p>
        <p class="text-slate-700 text-base md:text-lg leading-relaxed">{{ __('contact.email_label') }} <a href="mailto:{{ __('contact.email') }}" class="text-primary hover:underline">{{ __('contact.email') }}</a></p>
        <p class="text-gray-600 text-base md:text-lg leading-relaxed">{{ __('contact.office_hours_label') }} {{ __('contact.office_hours') }}</p>
        <p class="text-gray-600 text-base md:text-lg leading-relaxed mt-5">{{ __('contact.inquiries_message') }}</p>
    </div>
</div>
